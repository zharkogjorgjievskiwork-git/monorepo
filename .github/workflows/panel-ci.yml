name: panel-ci

on:
  pull_request:

jobs:
  lint-and-build:
    name: Lint & Build (React/TS/Vite)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: panel

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: panel/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
